<div class="post-preview">
    <a href="<%- config.root %><%- item.path %>">
        <h2 class="post-title">
            <%- item.title || theme.default_post_title || "Untitled" %>
        </h2>
        <% if (item.subtitle && item.subtitle.length) { %>
            <h3 class="post-subtitle">
                <%- item.subtitle %>
            </h3>
        <% } %>
    </a>
    <!-- <% if(item.excerpt) {%>
    <a class="post-excerpt" href="<%- config.root %><%- item.path %>">
        <%- item.excerpt %>
    </a>
    <% } %> -->
    <p class="post-meta">
        <!-- Date and Author -->
        <% if(item.author) { %>
            <%- item.author %> on
        <% } %>
        <%= item.date.format(config.date_format) %>
        <a href="<%- config.root %><%- item.path %>">继续阅读>></a>
    </p>
</div>
<hr>
